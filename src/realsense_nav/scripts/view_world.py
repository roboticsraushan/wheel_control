"""DEPRECATED: this script was replaced by the package module
`realsense_nav.view_world` (canonical). Keep this file only for
backwards-compatibility as a stub that advises usage of the installed
console script.

To run the viewer, prefer:

  source install/setup.bash
  ros2 run realsense_nav view_world

This script is intentionally a no-op placeholder.
"""

def main():
    raise RuntimeError("Deprecated: use 'ros2 run realsense_nav view_world' (the package-installed viewer)")


if __name__ == '__main__':
    main()
