# Placeholder EKF config for robot_localization to fuse ROVIO odom if desired
# Tune covariances after collecting data
frequency: 30.0

odom0: /odom/rovio
imu0: /imu/data

odom0_config: [true,  true,  false,
               false, false, true,
               false, false, false,
               false, false, false,
               false, false, false]

imu0_config: [false, false, false,
              true,  true,  true,
              false, false, false,
              false, false, false,
              false, false, false]

odom0_queue_size: 10
imu0_queue_size: 50
